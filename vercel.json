{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install",
  "buildCommand": "prisma generate && prisma migrate deploy && tsc -b && vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/index.js": {
      "maxDuration": 300,
      "memory": 1024,
      "includeFiles": "{server/**,node_modules/.prisma/client/**,node_modules/@prisma/client/**,node_modules/express/**,node_modules/cors/**,node_modules/bcryptjs/**,node_modules/jsonwebtoken/**,node_modules/multer/**,node_modules/dotenv/**,node_modules/@supabase/**,node_modules/pdfkit/**,prisma/schema.prisma,src/data/varsity/**}"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
